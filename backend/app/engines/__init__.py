"""Growth engines: Strategy, Marketing, Lead Gen, Sales, Analytics, Customer Success.

Each engine reads Company Memory, optionally enriches with web research, reasons
with the local LLM (or a grounded heuristic fallback), and saves a versioned
artifact. All share the recipe in `base.py`.
"""
